import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { useState } from "react";
import { useServerFn } from "@tanstack/react-start";
import { createPost } from "@/lib/social.functions";
import { PLATFORMS, type Platform } from "@/lib/platforms";
import { PlatformIcon } from "@/components/platform-badge";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Textarea } from "@/components/ui/textarea";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { toast } from "sonner";
import { ArrowLeft, CalendarClock, Check, ChevronRight, ImagePlus, Send, Sparkles } from "lucide-react";

export const Route = createFileRoute("/_authenticated/compose")({ head: () => ({ meta: [{ title: "Compose — Orbit" }] }), component: Compose });
function Compose() {
 const nav=useNavigate(), create=useServerFn(createPost); const [caption,setCaption]=useState(""); const [selected,setSelected]=useState<Platform[]>(["instagram","twitter"]); const [scheduledAt,setScheduledAt]=useState(""); const [saving,setSaving]=useState(false);
 const toggle=(p:Platform)=>setSelected(s=>s.includes(p)?s.filter(x=>x!==p):[...s,p]);
 async function save(status:"draft"|"scheduled"){if(!selected.length)return toast.error("Select at least one platform");if(status==="scheduled"&&!scheduledAt)return toast.error("Pick a date & time");setSaving(true);try{await create({data:{caption:caption.trim(),platforms:selected,status,scheduled_at:status==="scheduled"?new Date(scheduledAt).toISOString():null}});toast.success(status==="draft"?"Draft saved":"Post scheduled");nav({to:"/dashboard"})}catch(e){toast.error(e instanceof Error?e.message:"Failed")}finally{setSaving(false)}}
 return <div className="page-shell mx-auto w-full max-w-[1400px] space-y-6 p-4 sm:p-6 lg:p-8"><header className="flex items-start gap-3 border-b border-border/80 pb-6 sm:gap-4"><Button variant="ghost" size="icon" className="mt-0.5 shrink-0 rounded-lg" onClick={()=>nav({to:"/dashboard"})}><ArrowLeft className="h-4 w-4"/></Button><div className="min-w-0"><div className="flex items-center gap-2 text-[10px] font-bold uppercase tracking-[0.2em] text-primary sm:text-[11px]"><Sparkles className="h-3.5 w-3.5"/>Publishing</div><h1 className="mt-2 text-[28px] font-semibold tracking-[-0.045em] sm:text-[32px]">Create a post</h1><p className="mt-1.5 text-[13px] leading-6 text-muted-foreground sm:text-sm">Build once, then schedule it across your selected channels.</p></div></header>
 <div className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_400px]"><Card className="premium-card overflow-hidden"><CardContent className="p-0"><div className="border-b border-border/70 px-5 py-4 sm:px-6"><div className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-lg border border-primary/10 bg-primary/[0.07] text-primary"><Send className="h-4 w-4"/></div><div><h2 className="text-[13px] font-semibold">Post details</h2><p className="mt-1 text-[11px] text-muted-foreground">Choose where and what to publish.</p></div></div></div><div className="space-y-6 p-5 sm:p-6 lg:p-7"><div className="space-y-2.5"><Label>Publish to</Label><div className="grid gap-2 sm:grid-cols-2">{PLATFORMS.map(p=>{const active=selected.includes(p.id);return <button key={p.id} type="button" onClick={()=>toggle(p.id)} className={`group flex min-w-0 items-center gap-3 rounded-lg border p-3 text-left transition-colors ${active?"border-primary/35 bg-primary/[0.045] shadow-sm":"bg-background hover:border-primary/15 hover:bg-muted/30"}`}><span className={`flex h-8 w-8 shrink-0 items-center justify-center rounded-md ${active?"bg-primary text-primary-foreground":"border border-border bg-muted/50 text-muted-foreground"}`}><PlatformIcon platform={p.id} className="h-4 w-4"/></span><span className="min-w-0 flex-1 truncate text-sm font-medium">{p.name}</span>{active&&<Check className="h-4 w-4 shrink-0 text-primary"/>}</button>})}</div></div><div className="space-y-2.5"><div className="flex items-center justify-between"><Label htmlFor="caption">Caption</Label><span className="text-[11px] tabular-nums text-muted-foreground">{caption.length}/2200</span></div><Textarea id="caption" value={caption} onChange={e=>setCaption(e.target.value)} placeholder="Write something worth sharing…" rows={8} maxLength={2200} className="min-h-[220px] resize-none border-border/80 bg-background text-[15px] leading-6 shadow-none focus-visible:ring-2"/></div><button type="button" className="group flex w-full items-center gap-3 rounded-lg border border-dashed border-border bg-muted/[0.12] p-3.5 text-left hover:border-primary/25 hover:bg-primary/[0.02] sm:p-4"><div className="flex h-9 w-9 shrink-0 items-center justify-center rounded-lg border border-border bg-background"><ImagePlus className="h-4 w-4 text-muted-foreground"/></div><div className="min-w-0"><p className="text-sm font-medium">Add media</p><p className="truncate text-xs text-muted-foreground">Attach an image or video to your post.</p></div><ChevronRight className="ml-auto h-4 w-4 shrink-0 text-muted-foreground transition-transform group-hover:translate-x-0.5"/></button><div className="space-y-2.5"><Label htmlFor="when">Schedule</Label><div className="relative"><CalendarClock className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground"/><Input id="when" type="datetime-local" value={scheduledAt} onChange={e=>setScheduledAt(e.target.value)} className="pl-9"/></div><p className="text-xs text-muted-foreground">Leave the time empty if you only want to save a draft.</p></div></div><div className="flex flex-col-reverse gap-2 border-t border-border/70 bg-muted/[0.12] px-5 py-4 sm:flex-row sm:items-center sm:justify-between sm:px-6"><Button variant="outline" onClick={()=>save("draft")} disabled={saving} className="w-full sm:w-auto">Save draft</Button><Button onClick={()=>save("scheduled")} disabled={saving} className="w-full sm:w-auto">{saving?"Saving…":scheduledAt?"Schedule post":"Choose time to schedule"}<ChevronRight className="ml-1.5 h-4 w-4"/></Button></div></CardContent></Card>
 <div className="space-y-3 xl:sticky xl:top-4 xl:self-start"><div className="flex items-end justify-between gap-3"><div><h2 className="text-[13px] font-semibold">Live preview</h2><p className="mt-1 text-[11px] text-muted-foreground">A quick look at your selected channels.</p></div><span className="rounded-full border bg-muted/40 px-2.5 py-1 text-[10px] font-medium text-muted-foreground">{selected.length} selected</span></div>{selected.length===0?<Card className="premium-card"><CardContent className="flex min-h-48 items-center justify-center p-6 text-center text-sm text-muted-foreground">Select a platform to preview the post.</CardContent></Card>:selected.map(p=><Card key={p} className="premium-card overflow-hidden"><div className="flex items-center gap-3 border-b border-border/70 bg-muted/[0.16] px-4 py-3"><span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-md border border-border bg-background"><PlatformIcon platform={p} className="h-3.5 w-3.5"/></span><span className="text-xs font-semibold capitalize">{p}</span><span className="ml-auto text-[10px] text-muted-foreground">Preview</span></div><CardContent className="p-4"><div className="mb-3 h-28 rounded-lg border border-border/60 bg-muted/45"/><p className="whitespace-pre-wrap break-words text-sm leading-5">{caption||<span className="text-muted-foreground">Your caption will appear here…</span>}</p></CardContent></Card>)}</div></div></div>;
}
